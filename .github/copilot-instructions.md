# Copilot Instructions

## Python

Always use `uv` to run Python commands. For example:

- `uv run python script.py` instead of `python script.py`
- `uv run python -c "..."` instead of `python -c "..."`
